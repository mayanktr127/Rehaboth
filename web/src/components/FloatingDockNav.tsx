import React from 'react';

export const FloatingDockNav: React.FC = () => {
  // Website uses standard 3-dash mobile header navigation and breadcrumbs instead of app-style bottom dock
  return null;
};
